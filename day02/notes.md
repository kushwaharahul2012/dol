• What is a process?
process is a work after any task assighn to doing so its taking a time and doing some task under that time
• What is a service?
service is in linux its started with any event start up or any call this manages applications and function trigger by user.
• Foreground vs background jobs?
foreground is running directly in current user and background jobs runs behind the jobs not tie with foreground running process.

• What systemd does?
systemd is a default process which is init or start all process pid 1 its wake up all process in linux machine enviroment amd manages all processes.

• What a PID is and why it matters?

PID stands for process indentifier which is uniqe number assighn by the kernal to each running process.
• Difference between ps and top?
ps provide static data and top provides dynamic real time data.
